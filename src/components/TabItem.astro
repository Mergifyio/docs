---
/**
 * Individual tab panel used inside <Tabs>.
 *
 * Props:
 *   label - The tab button label text (required)
 */
export interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div data-tab-panel data-tab-label={label}>
  <slot />
</div>
