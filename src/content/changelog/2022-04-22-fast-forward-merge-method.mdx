---
title: fast-forward merge method
date: 2022-04-22
tags:
  - Merge Queue
  - Workflow Automation
description: "The `queue` and `merge` action supports the `fast-forward` merge method."
---

The `queue` and `merge` action supports the `fast-forward` merge method.
