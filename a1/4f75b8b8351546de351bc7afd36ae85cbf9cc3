Put the job_name removals back: the action's job_name input in MergifyCIUploadStepMatrix.astro and the Buildkite plugin's job_name property in BuildkiteCIUploadStepMatrix.astro. Both are declared and documented by their producers; they are dead because those producers export MERGIFY_JOB_NAME, which no uploader reads. That is a producer bug to fix in three repos, not a reason for the docs to steer readers to the env var and leave three READMEs disagreeing with us. The setup pages still name MERGIFY_TEST_JOB_NAME for setting the name by env, and the rest of the change — job-name-as-test-identity, per-provider defaults, the matrix explanation — is unchanged.
