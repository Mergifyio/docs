rebase onto #12538 and #12539. #12538 hardened the git diff | bazel query pipeline this change removes, so it resolves like the three before it — the page keeps this branch's side and the commit message says why deletions and renames need no special handling under bazel-diff. #12539 is different and is carried over: its empty-base check guards $BASE itself, which this recipe still uses (for git checkout rather than git diff), so both the GitHub Actions and Buildkite guards move across.
