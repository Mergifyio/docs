correct the image-check answer: I re-ran the OCR sweep and the existing denylist flags two of the three images, not all three — it misses detection.png and the still-live ci-insights/jobs.png, because a screenshot never renders <org>/<repo> as one token. Recommendation now says an image mode needs its own patterns; runtime measured at 40s for 84 images, not the 4 minutes first claimed
