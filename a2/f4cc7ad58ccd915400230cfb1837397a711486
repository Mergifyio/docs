rebased onto main after #12490 landed the same link fix inline; resolved the eleven conflicts to the partial and rewrote the message to describe what is left (the de-duplication and the two continue-on-error lines), and dropped the Fixes keyword since #12490 settled the ask
