docs#12535 landed on main while this was in flight: ScopesDetection now parses the refs with jq -er, so the null-base guard this block carried could never fire. Dropped it and folded the reason into the rev-parse comment. Also reworded two recipe comments that had the file/target ownership backwards.
