adopt the dashboard's current URL shape: org and repository live in the path (/orgs/<org>[/repos/<repo>]/...), not in ?login=&repository=. Review point from Alexandre — the query form is legacy and only resolves by redirect. Fixing the shape also fixes the leak more thoroughly, since a /orgs/ path cannot omit the org; and it corrected two wrong rows (/merge-queue resolves nowhere).
