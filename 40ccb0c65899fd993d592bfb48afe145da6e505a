address Copilot review: reword the 'Understanding Your Invoice' intro so it no longer implies an invoice has three sections — a regular invoice has two, and cancellation produces a separate final invoice
